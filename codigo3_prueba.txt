int main() {
    int counter = 0;
    int limit = 5;
    bool running = true;
    float sum = 0.0;

    while (running) {
        {
            counter = counter + 1;
            sum = sum + counter;
        
            if (counter >= limit) {
                running = false;
            }
        }
    }

    int max_value = 10;
    
    while (max_value > 0) {
        max_value = max_value - 1;
        
        if (max_value == 5) {
            bool paused = true;
        }
    }

    int i = 0;
    while (i < 3) {
        i = i + 1;
    }
    
    while (false) {
        int unreachable = 1;
    }

    float start = 1.0;
    while (start < 3.0) {
        start = start + 0.5;
    }
}
