int main;{
    int a = 10;
    float b = 5.5;
    bool flag = true;

    // Simple assignments
    int x = a;
    float y = b * 2.0;

    // Complex expression
    float result = (a + 5) * (y - b) / 2.0;
    
    // Assignment with type promotion (int to float)
    float promotion = 20;

    // Assignment of boolean
    bool is_valid = flag || false;

    // More arithmetic
    int sum = a + 20;
    int diff = 50 - a;
    int prod = sum * 2;
    float div_res = promotion / 2.5;

    // Using identifiers in expressions
    float final_value = result + div_res + a;

    // Integer division
    int quotient = 25 / 4;
    int remainder = 25 / 4;

    // Boolean assignment from comparison
    is_valid = (a > 5) && (b < 10.0);
    
    // Nested parenthesis in expression
    int complicated = ( (10 + 5) * 2 ) / 6;

    // Redeclaration (should cause error in semantic analysis)
    // int x = 1; // Commented out to be accepted by the compiler
}